# BarQR Generator

A simple web application built with Flask that generates **QR codes** and **Code128 barcodes** from user input.  
It includes a minimal backend API and a lightweight frontend interface.

## Features

- Generate QR codes
- Generate Code128 barcodes (supports alphanumeric text)
- Simple web UI
- Flask backend API
- Download generated images



